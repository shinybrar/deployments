apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "science-portal.fullname" . }}-test-connection"
  labels:
    {{- include "science-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "science-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
